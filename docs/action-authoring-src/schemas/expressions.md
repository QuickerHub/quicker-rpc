# {{#topic-title}}

**When**: P4 — default to **sys:assign** for single-var steps; `$=`/`$$` inline on other params; **sys:evalexpression** for batch/multi-var or LINQ. Keys from step_runner_get.

{{#include-partial expressions-body}}
