# Quicker expressions (quicker-eval-expression)

On-demand router — **QuickerAgent preloads `prompt-tier2.md` (expressions body) at session start.**

Deep-read when stuck: docs get **`evalexpression-examples`** · **`implementation-fallback`**.
