# --- 窗口与桌面 ---

写一个 把前台窗口设置为屏幕 1/2 比例大小并屏幕居中位置的 动作

写一个动作，显示或隐藏桌面图标

写一个动作：若前台窗口标题包含 "Visual Studio Code"，则将其最大化；否则弹出提示「当前不是 VS Code 窗口」

写一个动作，把前台窗口移动到第二块显示器并占满该屏幕工作区（多显示器场景）

写一个动作，切换前台窗口的「总在最前」状态（已置顶则取消，未置顶则置顶）

写一个动作：获取前台窗口标题和进程名，在文本窗口中显示，可复制

# --- 剪贴板与文本 ---

写一个动作：读取剪贴板文本，按行拆分后去重、去空行、排序，再写回剪贴板

写一个动作：把剪贴板里的 JSON 文本格式化（缩进 2 空格）后写回剪贴板；若不是合法 JSON 则提示错误原因

写一个动作：读取剪贴板文本，计算 MD5 哈希，在文本窗口显示原文与哈希值

写一个动作：把当前日期时间按「yyyy-MM-dd HH:mm:ss」格式写入剪贴板

写一个动作：等待剪贴板内容改变（最多 30 秒），然后将新内容追加写入桌面上的 clip-log.txt

写一个动作：读取剪贴板文本，统计字数、行数、非空行数，用表单展示并允许用户编辑后再写回剪贴板

# --- 文件与 Shell ---

写一个动作：用 Everything 搜索文件名包含用户输入关键词的文件，列出前 20 条，选中后在资源管理器中定位

写一个动作：把「下载」文件夹里过去 7 天下载的文件，按扩展名移动到子文件夹（如 pdf、jpg、zip、other）

写一个动作：读取剪贴板中的文件路径列表，打包为 zip 保存到桌面，文件名带时间戳

写一个动作：弹出表单让用户输入目录路径和文件后缀（如 .log），删除该目录下匹配的所有文件（删除前二次确认）

写一个动作：在桌面创建以当前日期命名的文件夹，并把剪贴板里的文件列表移动进去

# --- 表单与交互 ---

写一个动作：弹出多字段表单，收集「标题、标签（逗号分隔）、优先级（高/中/低）、备注」，确认后以 Markdown 清单格式写入剪贴板

写一个动作：让用户输入 URL，校验格式后以默认浏览器打开；无效 URL 时提示并允许重新输入

写一个动作：显示等待窗口「请切换到目标窗口后按确定」，确定后向活动窗口粘贴剪贴板内容

# --- 网络与 HTTP ---

写一个动作：请求 https://api.github.com/zen ，在文本窗口显示返回的 Zen 语句

写一个动作：用户输入网址，抓取页面 title 和 meta description，在文本窗口展示

写一个动作：把用户输入的文本 POST 到 https://httpbin.org/post ，在文本窗口显示响应 JSON 里的 json 字段

# --- 进程与系统 ---

写一个动作：检查 Notepad 是否已运行；已运行则激活其主窗口，未运行则启动 notepad.exe

写一个动作：获取前台进程信息（进程名、路径、窗口标题），若进程名在黑名单列表（如 WeChat.exe、Telegram.exe）中则提示「请勿在 IM 窗口使用」

写一个动作：截取前台窗口截图，保存到桌面，文件名含窗口标题与时间戳

# --- 列表、条件与变量 ---

写一个动作：预置一个数字列表 1..10，过滤出偶数，求和，结果显示在文本窗口

写一个动作：用户输入一个整数 N，若 N 为质数则提示「是质数」，否则分解质因数并显示

写一个动作：读取剪贴板多行文本为列表，每行前加「- [ ] 」前缀，合并后写回剪贴板（Todo 清单格式）

# --- 子程序 ---

写一个动作：搜索名为「打开网址」或类似的公共子程序，用子程序步骤打开用户输入的 URL（先 subprogram search/get，再 step-runner get）

写一个动作：调用合适的公共子程序，实现「清空剪贴板」功能（优先子程序，不要自己猜 callIdentifier）

# --- C# 脚本 / 无专用模块 ---

写一个动作：用 C# 脚本计算两个日期之间的工作日天数（排除周六日），表单输入起止日期，结果显示在文本窗口

写一个动作：用 C# 脚本生成 4 个 UUID，每行一个，写入剪贴板

写一个动作：用 C# 脚本读取环境变量 USERPROFILE 和 COMPUTERNAME，格式化输出到文本窗口

# --- WebView / 自定义界面 ---

写一个动作，用 webview 展示一个界面，上面是一个网页：一个面向团队项目管理的 SaaS 控制台首页。页面采用现代 B2B 产品风格，包含左侧可折叠导航栏、顶部全局搜索栏、通知中心、用户菜单、深浅色主题切换。主内容区展示项目总览仪表盘，包括项目进度卡片、任务燃尽图、成员工作负载、风险预警、近期活动时间线、待审批事项列表和跨团队日历。页面需要支持筛选项目、按成员/优先级/截止时间排序任务、拖拽任务卡片调整状态、打开任务详情抽屉、批量操作、导出报表，以及实时协作状态提示。整体布局清晰、信息密度高但不拥挤，使用蓝紫色渐变作为强调色，卡片有轻微阴影和圆角，交互状态丰富，适合桌面端 1440px 宽度展示。

写一个动作：用 WebView2 打开一个单页番茄钟（25 分钟倒计时、开始/暂停/重置、结束提示音可选），页面简洁现代

写一个动作：用 WebView2 展示剪贴板文本的 Markdown 预览（深色主题，支持标题/列表/代码块基本渲染）

写一个动作：用 WebView2 做一个「颜色选择器 + 复制 HEX/RGB」小工具页，选色结果写回 Quicker 变量并在关闭窗口后写入剪贴板

# --- 编辑已有动作（patch / 元数据）---

在 agent 作用域里找到你刚创建的动作，只修改它的图标和描述，不要改步骤（用 fa search 选合适图标）

找到标题包含「剪贴板」的 agent 动作，给它增加一步：在原有流程最后把结果同时写入 %USERPROFILE%\Desktop\qkrpc-last-output.txt

# --- 综合 / 压力 ---

写一个动作「智能粘贴」：若剪贴板是 URL 则提取域名写入；若是 JSON 则格式化后写入；若是纯数字则千分位格式化；否则原样粘贴到活动窗口。每步用条件分支，不要全堆在一个脚本里

写一个动作：表单输入搜索词 → Everything 搜索 → 若唯一匹配则直接打开所在文件夹并选中；若 0 条提示未找到；若多条则在文本窗口列出路径供复制

写一个动作：截图当前前台窗口 → OCR 识别文字 → 在文本窗口显示并可一键复制；识别失败时提示用户检查窗口内容是否清晰
