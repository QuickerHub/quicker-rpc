# CLI setup for agents

## P0

Quicker 运行中且已加载 QuickerRpc 插件。高频调用可先 `qkrpc serve`（默认 `http://127.0.0.1:9477`）。

## 入口

```powershell
qkrpc help --json
qkrpc guide get --topic overview --json
qkrpc guide get --topic authoring-workflow --json
```

## 专题

`overview` · `authoring-workflow` · `implementation-fallback` · `expressions` · `subprogram-workflow` · `step-runner-search` · `step-modules` · `patch-workflow` · `action-project-files`
