# B01 选中文本变换写回 — 2026-06-13

## 预期

`--param "hello world"` + `useActionParam` → `text` = `HELLO WORLD`，三步均成功。

## 实际

| 步骤 | 输出 |
|------|------|
| getSelectedText | `text` = `hello world`，`gotText` = True |
| stringProcess toUpper | `text` = `HELLO WORLD` |
| outputText paste | `gotText` = True |

动作 id：`86b87ec7-a0d2-4c19-ae6a-408f55bc55c7`（已删除）

## 卡点

无。`useActionParam` 是无头验证关键。

## 应对

pattern 文档与 draft skill 均记录 `useActionParam` + `action run --param`。

## 是否需改 skill

已产出 draft `quicker-authoring-selection-pipeline`；待用户审核晋升。
