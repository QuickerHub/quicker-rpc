# B02 HTTP GET JSON 提取 — 2026-06-13

## 预期

GET `https://httpbin.org/json` → `author` = `Yours Truly`，`statusCode` = 200。

## 实际

| 步骤 | 输出 |
|------|------|
| http GET | `statusCode` = 200，`respBody` 含 slideshow JSON |
| jsonExtract p0=slideshow.author | `author` = `Yours Truly` |

动作 id：`69e84daf-9477-4840-baea-6923abdd8967`（已删除）

## 卡点

无。http 步骤约 3.9s（网络延迟）。

## 应对

生产动作可酌情增大 `expireSeconds`；测试用公共 API 即可。

## 是否需改 skill

已产出 draft `quicker-authoring-http-json-api`；待用户审核晋升。
