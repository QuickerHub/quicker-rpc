# B04 子程序复用 — 2026-06-13

## 预期

global 子程序将 `text` 转大写 → 主动作 `outputText` = `HELLO SUB`。

## 实际

| 阶段 | 输出 |
|------|------|
| subprogram patch | `stringProcess toUpper` on `text` |
| 首次 caller（`text.var`） | IO 未传入，showText 空 |
| 修正 `var:text` / `var:result` | `hello sub` → `HELLO SUB` ✅ |

动作 `b4b9c75d-…`、子程序 `b6675a14-…` 已删除。

## 卡点

**子程序 IO wire 不是 `text.var`**，而是 **`var:text`**（输入）与 **`var:result`**（输出）。`step-runner get` 静态 schema 无 `var:*`，patch 会 warning 但运行有效。

## 应对

pattern / skill / `.local/patch-b04-caller.json` 已改为 `var:` 前缀；参照 `voxtype-run-subprogram.patch.json`。

## 是否需改 skill

`quicker-authoring-subprogram-extract` → **review**。
