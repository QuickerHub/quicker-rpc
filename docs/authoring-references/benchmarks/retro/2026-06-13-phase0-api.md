# Phase 0 API 验收 — 2026-06-13

> CLI：`publish/cli/qkrpc.exe`（热更新后）· 插件：0.14.4.59+

## 验收结果

| 项 | 命令 | 结果 |
|----|------|------|
| library search | `action library search --keyword 选中文本 --limit 2` | ✅ `totalCount:1473`，含 `875ef658-…` |
| shared get | `action shared get --id 875ef658-… --return-mode full` | ✅ `readOnly:true`, `patchAllowed:false`, 3 步 |
| shared patch 拒绝 | `patch --id 875ef658-…` | ✅ `READ_ONLY_SHARED_ACTION` |
| library patch 拒绝 | `patch --id a84ed4e1-…`（`--filter library`） | ✅ `READ_ONLY_LIBRARY_ACTION` |

## 构建备注

- 首次 `build.ps1 -t`：CLI 发布目录被锁（`clrjit.dll`）；`MockProfileLoader.cs` `StartsWith('_')` 编译错误已修。
- 二次 `build.ps1 -t`：插件 qkbuild 报 `TryInvokeStepListPaste` 重复（`dotnet build` 单独通过）。
- 最终：`dotnet publish` → `publish/cli` 可用；`publish-rpc.ps1` setup 步骤失败不影响 CLI 验收。

## Phase 1 续

- `clipboard-pipeline` pattern + draft skill + trace 验证（已删临时动作 `ee9a6d10-…`）。
