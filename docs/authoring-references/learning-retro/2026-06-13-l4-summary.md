# L4 汇总 — 2026-06-13

> 基于 B01–B03 trace + Phase0 API + SDK L2 retro；B04/B05 待 RPC 恢复。

## 重复卡点（应写入 skill / prompt）

| 卡点 | 出现处 | 应对 |
|------|--------|------|
| `number` 变量字面量 `{a}=1` 失败 | SDK multi-var | `Convert.ToDouble(n)` |
| `sys:if` 无 Else → NullReference | B03 | 单分支用 `simpleIf` |
| `each` 子步骤位置错误 | B03 | 写在 `ifSteps` |
| `writeClipboard` text vs input 键 | clipboard-pipeline | `type:text` → `text.var` |
| 库/分享动作 patch | Phase0 | `READ_ONLY_*`，`shared get` 只读 |
| 子程序 IO 用 `text.var` | B04 首次失败 | **`var:text` / `var:result`** |

## 晋升建议（需你审核）

| skill | 状态 | verifiedBy | 建议 |
|-------|------|------------|------|
| quicker-authoring-selection-pipeline | review | B01 | **晋升** |
| quicker-authoring-http-json-api | review | B02 | **晋升** |
| quicker-action-library-search | review | Phase0 API | **晋升**（P3 docs:gen 可选） |
| quicker-authoring-clipboard-pipeline | review | trace | **晋升** |
| quicker-authoring-loop-control | review | B03 | **晋升** |
| quicker-authoring-evalexpression-multi-var | review | SDK+B03 | **晋升**（陷阱子 skill） |
| quicker-authoring-subprogram-extract | review | B04 | **晋升** |
| quicker-authoring-file-batch | review | B05 | **晋升** |

晋升路径见 `learned-skills/SPEC.md` §6。

## 进度快照

- L2 pattern：**6/10** done
- L3 benchmark：**5/5** done ✅
- L4 retro：第 1 轮；**8** skill 待审核晋升

## 下一步

1. 你审核 → **8** 个 skill 晋升 `docs/action-authoring-src/skills/` + `docs:gen`
2. 续跑 pattern：expression-first、path-and-exists（batch 2–3）
3. 可选：authoring-tasks 增 L0 `discover-library-search`
