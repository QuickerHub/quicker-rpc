# L4 汇总 — 2026-06-13

> 基于 B01–B05 trace + Phase0 API + SDK L2 + L2 pattern 10/10。

## 重复卡点（应写入 skill / prompt）

| 卡点 | 出现处 | 应对 |
|------|--------|------|
| `number` 变量字面量 `{a}=1` 失败 | SDK multi-var | `Convert.ToDouble(n)` |
| `sys:if` 无 Else → NullReference | B03 | 单分支用 `simpleIf` |
| `each` / `repeat` 子步骤位置错误 | B03, delay-retry | 写在 **`ifSteps`** |
| `writeClipboard` text vs input 键 | clipboard-pipeline | `type:text` → `text.var` |
| 库/分享动作 patch | Phase0 | `READ_ONLY_*`，`shared get` 只读 |
| 子程序 IO 用 `text.var` | B04 首次失败 | **`var:text` / `var:result`** |
| `checkPathExists` 输出键 | path-and-exists | **`isExists`**，非 `isSuccess` |
| `keyInput` 在 Electron/IDE | ui-automation-lite | 试 `sendKeys` 或 Win32 目标 |
| 勿激活 Quicker 自身 | ui-automation-lite | 换其他 `process` + `path` |

## Skill 单源（2026-06 改版）

学习产出**直接**写入 `docs/action-authoring-src/skills/<skillName>/`，经 `docs:gen` 发布到 `docs/skills/`。  
**禁止**再写 `.cursor/skills/quicker-authoring-*`。审核门控见 `learned-skills/registry.json` 的 `status`。

当前 12 个场景 skill 已在 src + registry `promoted`。规范：[learned-skills/SPEC.md](../learned-skills/SPEC.md)。

## 进度快照

- L2 pattern：**10/10** done ✅
- L3 benchmark：**5/5** done ✅
- L4 retro：第 1 轮；**12** skill 在 `action-authoring-src/skills/`（单源）

## 下一步

1. ~~`authoring-tasks` L0 `discover-library-search`~~ ✅
2. ~~`subprogram.md` 补 `var:*` IO~~ ✅
3. 扩展 L2 pattern backlog 或跑 `authoring-tasks` 全量手动评测
