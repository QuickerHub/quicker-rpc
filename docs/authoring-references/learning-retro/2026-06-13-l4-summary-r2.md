# L4 汇总 — 2026-06-13（第 2 轮）

> Batch 5–6：+6 pattern，**18** 场景 skill promoted。

## 本轮新卡点

| 卡点 | 出现处 | 应对 |
|------|--------|------|
| `regexExtract` `match1` wire | batch5 regex | **`match1 `** 尾随空格（从 step-runner get 复制） |
| `sys:form` headless | form-param-input | **trace-exempt**；长表单 `formDef.file` |
| `simpleIf` else 不执行 | conditional-http 首版 | else 含 **http/多步** → **`sys:if`** |
| 最大化窗口 | window-title-branch | `windowOperations` **`type: show`** + **`showCmd: 3`** |
| `fileOperation` 复制重命名 | file-copy-timestamp | **`copyTo`**（完整 dstPath），非 `copyInto` |

## 进度快照

- L2 pattern：**16/16** done（首批 10 + batch5×3 + batch6×3）
- L3 benchmark：**5/5** done
- 场景 skill：**18** promoted（`docs:gen` 校验）
- L4 retro：**2** 轮

## 与 authoring-tasks 对齐

| task id | pattern |
|---------|---------|
| `conditional-http-cache` | conditional-http ✅ |
| `file-copy-timestamp` | file-copy-timestamp ✅ |
| `window-vscode-branch` | window-title-branch ✅ |
| `form-to-clipboard` | form-param-input ✅（UI trace-exempt） |

## 下一步

1. backlog：`external-eval-cs`、`csv-parse-aggregate`
2. 可选：`authoring-tasks.json` 全量手动评测记分
3. 可选：修正 step-runner schema 中 `match1 ` 尾随空格（产品侧）
