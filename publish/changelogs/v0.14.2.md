v0.14.2

QuickerAgent

- **修复桌面版无法启动**：Electron 主进程缺少 `browser-runtime/protocol.mjs`（`app.asar` 未打包 browser-runtime），手动从开始菜单启动会静默退出
- 打包校验：`electron-shell-staging` 同步 browser-runtime；`verify-electron-asar` 检查 asar 体积

插件 / Quicker

- **quicker.rpc 0.14.2.0**：`Launcher` DI Host 改为延迟初始化，避免 QExpr `load` 路径下 Costura 未就绪即创建 Host
- `QuickerRpc_Run` 子程序 `version` 同步为 **0.14.2.0**
