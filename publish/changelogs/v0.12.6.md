v0.12.6

CLI

- 程序语法 lint 扩展：**分支结构**（`ProgramBranchStructureLint`）、**输入脚本语法**（`InputScriptSyntaxLint`）及 step-branch wire 规范化
- 表达式执行：变量占位符与 `EvalExpressionStepRunner` 改进，减少复杂表达式误报
- 动作编写文档：step-module 参考迁入 `authored/` 目录，schema 主题合并进 `action-data-schema`

安装

- GitHub Release：`qkrpc-win-x64-setup.exe` / 便携 zip（CI 构建）
- QuickerAgent：`quicker-agent-{semver}-x64-setup.exe`（GitHub Release + Bitiful 镜像）

插件 / Quicker

- 版本号 **0.12.6.0**（与 `quicker.rpc` 依赖包一致，第三段正式发布）
- QuickerAgent：Tauri 原生嵌入式浏览器（bounds 同步）、LLM endpoint 探测、web search 工具、dev 恢复与启动脚本改进
- 分享动作更新说明与本 Release 一致
