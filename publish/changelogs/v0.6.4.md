v0.6.4

CLI
- 与 v0.6.0 相同 CLI 功能；本版主要为插件稳定性与监控窗体验修复

安装
- `qkrpc-win-x64-setup.exe` + zip 便携包；Release 与 CI 共用本 changelog

插件 / Quicker
- **监控窗剪贴板**：复制 ActionId / 动作元数据改用 Quicker `ClipboardHelper`（次级 STA），并延迟到 UI 空闲后写入，减少 DataGrid / 右键菜单占用剪贴板导致的 OpenClipboard 失败
- **监控窗交互**：ActionId 列左键复制改为 Preview 命中检测；元数据复制同样延后调度；窗口独立居中显示（不再 Owner 到 Quicker 主窗）
- 文档：`QuickerRpc_Run` 入参 `openMonitor`、虚拟动作页 **QuickerRpc 管理** 打开监控窗的说明（AGENTS / README）
