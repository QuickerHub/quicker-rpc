v0.11.1

CLI

- 版本号 0.11.1.0（与 `quicker.rpc` 依赖包一致）

安装

- GitHub Release：`qkrpc-win-x64-setup.exe`、便携 zip、**QuickerAgent** Tauri 安装包
- 修复 Release CI `build-agent` 在 Post Rust cache 步骤失败导致 Release 未发布的问题
- 修复 ForceRetag 与并发 workflow 导致的 `actions/checkout` tag/commit 不一致

agent-gui（QuickerAgent）

- Tauri Release 构建修复：TypeScript、voice 插件元数据 staging、Next trace 排除
- Release 构建：`lib.rs` debug/release 分支与 `voice-plugin-channel.json` 反序列化
- 文档生成：`topics.json` 跨平台稳定排序；docs-check 支持手动触发

插件 / Quicker

- `quicker.rpc` 依赖包同步至 0.11.1
- 分享动作更新说明与本 Release 一致
