v0.7.1

## CLI

- （本版无 CLI 行为变更）

## 安装

- GitHub Actions `release-cli` 同步构建 **QuickerAgent** Tauri NSIS 安装包（`QuickerAgent_{版本}_x64-setup.exe` 与 `quicker-agent-win-x64-setup.exe` latest 别名）
- 发布脚本默认仅 push tag，由 CI 上传 qkrpc zip/setup 与 Agent 安装包

## 插件

- （本版无插件 RPC 行为变更）

## agent-gui

- 发布构建可嵌入 `BUNDLED_LLM_AI98PRO_API_KEY`（混淆密钥，不入库），并支持用户侧 LLM 提供商配置
- 自动对话标题、Tauri 无边框/overlay 标题栏与窗口控件、主题与侧栏体验优化
- 首次打开默认收起左侧对话列表，展开偏好写入 localStorage
- 修复 Next.js 主题模块 RSC 边界导致的构建错误；Tauri API 仅客户端加载
