v0.12.7

CLI

- 动作编写文档：从 QuickerModuleDoc 蒸馏 step-module JSON 范例，压缩时省略与 schema 默认相同的 `inputParams`
- `step-runner get`：Boolean 参数默认值输出 JSON `true`/`false`；`inputParams` wire 支持 bool、number、array、object 字面量
- `action-data-schema`：`inputParams` 值类型与 List/Dict 数组/对象 wire 规则对齐

安装

- GitHub Release：`qkrpc-win-x64-setup.exe` / 便携 zip（CI 构建）
- QuickerAgent：`quicker-agent-{semver}-x64-setup.exe`（GitHub Release + Bitiful 镜像）

插件 / Quicker

- 版本号 **0.12.7.0**（与 `quicker.rpc` 依赖包一致，第三段正式发布）
- AgentModel：`InputParamWireCoercer` 保留类型化 wire；schema 默认比较统一 bool 别名
- QuickerAgent：AskQuestion 停靠、启动器查询解析、内联动作 trace；语音 runtime 引导与异步模型下载；远程 LLM 模型选择与 endpoint 固定；聊天会话自动恢复
- 分享动作更新说明与本 Release 一致
