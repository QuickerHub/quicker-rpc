v0.12.10

CLI

- 与 v0.12.9 相同：动作编写文档蒸馏范例、typed `inputParams` wire、Boolean schema 默认值

安装

- GitHub Release：`qkrpc-win-x64-setup.exe` / 便携 zip（CI 构建）
- QuickerAgent：`quicker-agent-{semver}-x64-setup.exe`（GitHub Release + Bitiful 镜像）
- 修复 NSIS 安装 hook 找不到 `kill-bundled-node.vbs` 导致 CI `build-agent` 失败

插件 / Quicker

- 版本号 **0.12.10.0**（与 `quicker.rpc` 依赖包一致，第三段正式发布）
- QuickerAgent：Release `next build` 类型修复；动作编写文档搜索别名与 eval；workspace grep eval
- 语音插件 channel 同步至 voice-asr-runtime **v0.1.3**
- 分享动作更新说明与本 Release 一致
