v0.12.8

CLI

- 与 v0.12.7 相同：动作编写文档蒸馏范例、typed `inputParams` wire、Boolean schema 默认值

安装

- GitHub Release：`qkrpc-win-x64-setup.exe` / 便携 zip（CI 构建）
- QuickerAgent：`quicker-agent-{semver}-x64-setup.exe`（GitHub Release + Bitiful 镜像）
- 修复 NSIS 安装前停止 bundled `node.exe` 的 PowerShell 命令（避免 `StrCpy` 解析失败）
- 发布前 `preflight-release-gate` 快速校验（launcher 快捷键契约 + 回归测试）

插件 / Quicker

- 版本号 **0.12.8.0**（与 `quicker.rpc` 依赖包一致，第三段正式发布）
- QuickerAgent：修复 Release `next build` 类型错误；launcher 默认快捷键 Alt+Space（TS/Rust 契约测试）；LLM endpoint probe / 模型选择器类型修正
- 分享动作更新说明与本 Release 一致
