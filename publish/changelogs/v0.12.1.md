v0.12.1

插件 / Quicker

- 版本号 **0.12.1.0**（与 `quicker.rpc` 依赖包一致）
- **Costura 重新嵌入 `QuickerRpc.Contracts` / `QuickerRpc.AgentModel`**：依赖包仅发布单个 `QuickerRpc.Plugin.*.dll`，不再附带版本后缀卫星 DLL
- `build.yaml` 同步：`filesToInclude` 仅保留 `QuickerRpc.Plugin*.dll`
- `quicker.rpc` 依赖包同步至 **0.12.1.0**
- 分享动作更新说明与本 Release 一致
