v0.7.2

## CLI

- （本版无 CLI 行为变更）

## 安装

- （沿用 v0.7.1：CI 发布 qkrpc 安装包与 QuickerAgent Tauri NSIS）

## 插件

- （本版无插件 RPC 行为变更）

## agent-gui

- **默认工作目录**：开发环境为 quicker-rpc 仓库根；安装版为 `Documents/QuickerAgent`（自动创建）；支持 `AGENT_GUI_DEFAULT_CWD` 覆盖
- **工作目录设置**：侧栏改为应用内对话框；Tauri 下可「浏览…」选择文件夹（`plugin-dialog`）
- **Tauri 标题栏**：左右留白拖动窗口；标签左对齐；关闭按钮贴右缘；启动时窗口居中
- **开发体验**：`tauri dev` / `pnpm dev` 默认不再自动打开系统浏览器
- 修复 Tauri 下标题栏 SSR hydration 不匹配
- 修复 CI 构建 QuickerAgent 时 Next standalone 误扫描 runner `Documents` 目录
