v0.12.11

CLI

- 第三方 Agent MCP 统一走 `workspace_program`：动作/子程序/同步工具合并为通用读写接口，新增 `qkrpc agent setup` 自安装指引
- MCP 工具目录与文档对齐新 authoring 模型；移除旧 `qkrpc_sync` 专用工具面

安装

- GitHub Release：`qkrpc-win-x64-setup.exe` / 便携 zip（CI 构建）
- QuickerAgent：`quicker-agent-{semver}-x64-setup.exe`（GitHub Release + Bitiful 镜像）
- Plugin Runtime Gallery：语音 ASR runtime 可独立于主程序发版更新（远程 channel + registry）

插件 / Quicker

- 版本号 **0.12.11.0**（与 `quicker.rpc` 依赖包一致，第三段正式发布）
- QuickerAgent：Composer 消息队列；启动流程与安装探测优化；聊天滚动与冷启动展开体验
- 语音插件：远程 `voice-plugin-channel.json` 拉取与 6h 缓存；设置页支持检查/安装 Runtime 更新
- voice-asr-runtime 同步至 **0.1.3**（SenseVoice model identity 打包修复）
- 分享动作更新说明与本 Release 一致
