v0.11.2

CLI

- 版本号 0.11.2.0（与 `quicker.rpc` 依赖包一致）

安装

- **热修复**：QuickerAgent 0.11.1 安装后白屏/卡死（内嵌 Next.js 服务 500）

agent-gui（QuickerAgent）

- 修复 Tauri standalone 打包缺少 `app-route` 等 Next.js 运行时，导致安装版所有 HTTP 请求 500
- `tauri-prepare`：合并完整 `next-server` / `app-render` 模块；启动时重写 monorepo 绝对路径
- `verify-tauri-bundle`：校验 API route 运行时文件存在

插件 / Quicker

- `quicker.rpc` 依赖包同步至 0.11.2
- 分享动作更新说明与本 Release 一致
