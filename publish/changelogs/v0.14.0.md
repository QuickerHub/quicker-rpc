v0.14.0

CLI

- **触发器任务 RPC**：事件目录与服务；`trigger` CLI / MCP / serve 分发端点
- **浏览器脚本**：`browser-runtime` 增加 `evaluate-script`、`page-search`、`page-structure`；Playwright 回退
- **运行时工具**：`runtime-compile`、`runtime-benchmark` CLI；agent-gui argv 解析与 authoring benchmark 任务
- **校验增强**：serve `validate` 暴露 `schemaIssues`，便于 patch 错误定位

安装

- GitHub Release：`qkrpc-win-x64-setup.exe` / 便携 zip（CI 构建）
- QuickerAgent：`quicker-agent-{semver}-x64-setup.exe`（GitHub Release + Bitiful 镜像）
- Electron 安装包资源与发布流程统一；CI release upload 修复 `github.token` 传递

插件 / Quicker

- 版本号 **0.14.0.0**（与 `quicker.rpc` 依赖包一致，第三段正式发布）

QuickerAgent

- **Electron 壳**：独立桌面安装包、ChromeControl MCP、流式代码预览
- **Slash 菜单**：扩展 skills/agents 目录；标准化 agent 定义、子代理与 slash 命令
- **启动器**：快捷键与 transcript UI 调整
- **嵌入式终端**：多标签 / multiplexer 重构
- **文档**：authoring-references 拆分；docs 搜索 snippet 统一
