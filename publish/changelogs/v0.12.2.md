v0.12.2

插件 / Quicker

- 版本号 **0.12.2.0**（与 `quicker.rpc` 依赖包一致）
- **Costura 单文件**：`QuickerRpc.Plugin` 重新嵌入 `QuickerRpc.Contracts` / `QuickerRpc.AgentModel`，依赖包仅含单个 `QuickerRpc.Plugin.*.dll`
- **QEE / QAL 加载修复**：QExpr `PreloadAssembly`（不再全类型 `RegisterAssembly`）；QAsmReg 使用 `AssemblyLoadCache.GetOrLoad`
- **QuickerAgent 右键菜单**：「退出 Agent（强制关闭 quicker-agent）」→ `agent-kill`，仅终止 Agent 进程，不关闭 RPC
- `Launcher.StartFromQuickerInParam`：统一解析 `quicker_in_param`（含 `agent-kill`）
- `quicker.rpc` 依赖包同步至 **0.12.2.0**
- 分享动作更新说明与本 Release 一致
