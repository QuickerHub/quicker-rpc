v0.14.7

QuickerAgent

- **浏览器自动化**：Playwright 无头、Electron 离屏嵌入、Connector 扩展三路路由；`user_browser` 工具注册与目标路由 UI
- **上下文压缩 v2**：token 预算与 API round 边界；L1 microcompact、L2 结构化摘要、reactive compact；压缩后 reinject 近期 workspace 片段
- **动作设计器**：表达式编辑器由 Monaco 换为轻量 CodeMirror（Quicker 语法高亮、多行高度与滚动链修复）
- **聊天与工作区**：紧凑动作条、portal 菜单、shell 终端 UX、完整文件 diff、hunk 锚定；grep/host/trigger/docs 工具结果视图
- **Agent eval**：Runner B 场景、nightly/unit CI；step-runner browse catalog、designer 搜索 HTTP API
- **侧栏与面板**：文件夹图标复用、线程时间 hydration 修复；浏览器 runtime 徽章与 panel sync 优化

CLI / 插件

- **ActionRuntime**：mock profile 子程序桩；子模块更新（compile-verify 合并、CodegenVersion、ResolveManifestPath）
- **插值校验**：lint 行定位器；step-input 校验与 program-data reload 改进
- **qkrpc serve**：延迟测试；trigger-workflow 编写文档
- **quicker.rpc 0.14.7.0**：与 QuickerAgent 0.14.7 同步发布
- `QuickerRpc_Run` 子程序 `version` 同步为 **0.14.7.0**
