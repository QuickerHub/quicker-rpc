/** Must match `WORKSPACE_BROWSER_WEBVIEW_LABEL` in agent-gui frontend. */
export const WORKSPACE_BROWSER_LABEL = "workspace-browser";

/** Relative profile directory (matches Tauri `EMBEDDED_BROWSER_DATA_DIRECTORY`). */
export const EMBEDDED_BROWSER_DATA_DIRECTORY = "profile";

export const ELECTRON_APP_IDENTIFIER = "ai.quicker.agent.electron";

/** Tauri WebView2 user-data identifier (shared profile root for embedded browser). */
export const TAURI_WEBVIEW_IDENTIFIER = "ai.quicker.agent";
