# QuickerBench empty fixture

Minimal workspace for QuickerBench eval: only an empty `.quicker/` tree.
Used by agent-eval and `/tool-test` QuickerBench tab so agents start without local action projects.
