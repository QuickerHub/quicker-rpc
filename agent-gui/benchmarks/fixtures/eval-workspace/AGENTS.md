# Eval fixture workspace

This directory is used by agent-gui eval harness (`benchmarks/agent-gui-scenarios.json`).

- Slash command `/list-actions` is defined under `.quicker/commands/`.
- Subagent `readonly-explore` is under `.quicker/agents/`.
- Do not store secrets here.
