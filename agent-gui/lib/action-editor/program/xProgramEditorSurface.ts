/**
 * Which program document an XProgramEditor instance edits.
 * Used to show or hide variable detail fields that only apply inside a sub program.
 */
export type XProgramEditorSurface = "main" | "subProgram";
