/** Emitted by Tauri when the launcher webview window is hidden (blur, Esc, etc.). */
export const LAUNCHER_HIDDEN_EVENT = "launcher:hidden";

/** Emitted when the launcher window gains focus / is shown. */
export const LAUNCHER_SHOWN_EVENT = "launcher:shown";
